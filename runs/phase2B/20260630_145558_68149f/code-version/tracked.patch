diff --git a/.gitignore b/.gitignore
index a2a2da3..590b6ff 100644
--- a/.gitignore
+++ b/.gitignore
@@ -43,6 +43,19 @@ Icon?
 .env
 .env.local
 .env.old
+
+# --- Local working notes ---
+todos.md
+infoforclaude.txt
+CLAUDE.md
 Phasen_KNOWHOW
 phase1notes
 phase2notes
+analyse
+commit-rules.md
+NextStep
+
+# --- Analysis prose (write thesis yourself; cite raw runs/results, not this) ---
+# Keeps raw evidence committed (thesis-evidence/**/*.json + *-labels.md), excludes only the summaries.
+thesis-evidence
+input/eval-labels
\ No newline at end of file
diff --git a/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimEvidenceVerifier.cs b/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimEvidenceVerifier.cs
index 45e48da..cf02dc2 100644
--- a/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimEvidenceVerifier.cs
+++ b/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimEvidenceVerifier.cs
@@ -11,7 +11,9 @@ public sealed record ClaimEvidenceCase(
     string ExpectedLabel,
     IReadOnlyList<string> Evidence,
     string? Note = null,
-    string? OriginHandLabel = null);
+    string? OriginHandLabel = null,
+    string? ArtifactQuote = null,
+    IReadOnlyDictionary<string, string>? Facets = null);
 
 public sealed record ClaimEvidenceVerdict(
     string Id,
@@ -31,8 +33,9 @@ public sealed class ClaimEvidenceVerifier
 
     private const string SystemPrompt = """
         Du pruefst EINEN atomaren Claim eines SDLC-Artefakts gegen die explizit zugeordnete Evidence.
-        Du bekommst NICHT das ganze Transkript, sondern nur Claim + Evidence-Spans. Entscheide streng,
-        ob genau diese Evidence genau diesen Claim traegt.
+        Du bekommst NICHT das ganze Transkript, sondern nur Claim + Evidence-Spans. Optional bekommst du
+        die Original-Quote und erhaltene Facetten (Komponente, Akteur, Aktion, Objekt, Modalitaet, Status,
+        Scope). Entscheide streng, ob genau diese Evidence genau diesen Claim UND seine Facetten traegt.
 
         Pruefe lokal:
         - Wird der Claim explizit gestuetzt?
@@ -40,6 +43,8 @@ public sealed class ClaimEvidenceVerifier
         - Ist er nur eine Inferenz? Wenn ja: sind die Praemissen ausreichend?
         - Wurden Modalitaet, Status, Scope oder Zeitbezug verschaerft?
         - Fuegt der Claim Entitaeten, Technologien, Zahlen oder Eigenschaften hinzu, die die Evidence nicht traegt?
+        - Wenn der fachliche Kern getragen ist, aber eine Facette (z. B. Frontend/UI, finaler Status, MVP-Scope)
+          nicht getragen ist: waehle eher overstated/support=partial statt unsupported.
         - Widerspricht der Claim der Evidence?
 
         VERDIKTE:
@@ -103,12 +108,29 @@ public sealed class ClaimEvidenceVerifier
     public async Task<ClaimEvidenceVerdict> VerifyAsync(ClaimEvidenceCase testCase, CancellationToken ct)
     {
         var evidence = string.Join("\n", testCase.Evidence.Select((e, i) => $"E{i + 1}: {e}"));
+        var artifactQuote = string.IsNullOrWhiteSpace(testCase.ArtifactQuote)
+            ? ""
+            : $"""
+
+            ORIGINAL-QUOTE:
+            {testCase.ArtifactQuote}
+            """;
+        var facets = testCase.Facets is { Count: > 0 }
+            ? $"""
+
+            ERHALTENE FACETTEN:
+            {string.Join("\n", testCase.Facets.Select(kv => $"- {kv.Key}: {kv.Value}"))}
+            """
+            : "";
+
         var user = $"""
             ID: {testCase.Id}
             ARTEFAKT: {testCase.Artifact}
 
             CLAIM:
             {testCase.Claim}
+            {artifactQuote}
+            {facets}
 
             EVIDENCE:
             {evidence}
diff --git a/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimSplitSpikeRunner.cs b/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimSplitSpikeRunner.cs
index ad418ee..b43d3f8 100644
--- a/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimSplitSpikeRunner.cs
+++ b/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimSplitSpikeRunner.cs
@@ -66,26 +66,52 @@ public static class ClaimSplitSpikeRunner
         var results = new List<object>();
         foreach (var u in units)
         {
-            var atomics = await splitter.SplitAsync(u.Unit, u.Artifact, CancellationToken.None).ConfigureAwait(false);
+            var atomics = await splitter.SplitDetailedAsync(u.Unit, u.Artifact, CancellationToken.None).ConfigureAwait(false);
             Console.WriteLine($"\n[UNIT] {u.Id} ({u.Artifact}) → {atomics.Count} atomare Claims");
 
             var claimResults = new List<object>();
             for (var i = 0; i < atomics.Count; i++)
             {
-                var claim = atomics[i];
+                var atomic = atomics[i];
+                var claim = atomic.NormalizedClaim;
                 if (!fullChain)
                 {
                     Console.WriteLine($"   C{i + 1}: {claim}");
-                    claimResults.Add(new { claim });
+                    claimResults.Add(new
+                    {
+                        claim,
+                        atomic.ArtifactQuote,
+                        facets = atomic.Facets,
+                        atomic.SplitReason
+                    });
                     continue;
                 }
 
                 var sel = await selector.SelectAsync(claim, u.Artifact, turns, CancellationToken.None).ConfigureAwait(false);
                 var evidence = EvidenceSelector.ToEvidence(sel, turns);
                 var verdict = await verifier.VerifyAsync(
-                    new ClaimEvidenceCase($"{u.Id}-C{i + 1}", u.Artifact, claim, "n/a", evidence), CancellationToken.None).ConfigureAwait(false);
+                    new ClaimEvidenceCase(
+                        $"{u.Id}-C{i + 1}",
+                        u.Artifact,
+                        claim,
+                        "n/a",
+                        evidence,
+                        ArtifactQuote: atomic.ArtifactQuote,
+                        Facets: atomic.Facets.ToDictionary()),
+                    CancellationToken.None).ConfigureAwait(false);
                 Console.WriteLine($"   C{i + 1} [{verdict.Label}/{verdict.Verdict}] turns=[{string.Join(",", sel.Turns)}]: {claim}");
-                claimResults.Add(new { claim, selectedTurns = sel.Turns, verdict.Label, verdict.Verdict, verdict.Support, verdict.Reason });
+                claimResults.Add(new
+                {
+                    claim,
+                    atomic.ArtifactQuote,
+                    facets = atomic.Facets,
+                    atomic.SplitReason,
+                    selectedTurns = sel.Turns,
+                    verdict.Label,
+                    verdict.Verdict,
+                    verdict.Support,
+                    verdict.Reason
+                });
             }
 
             results.Add(new { u.Id, u.Artifact, u.Unit, u.Note, atomCount = atomics.Count, claims = claimResults });
diff --git a/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimSplitter.cs b/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimSplitter.cs
index f1b9f8b..bb1ebd0 100644
--- a/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimSplitter.cs
+++ b/AgenticSdlc.Host/Phases/Phase2/Evaluation/PerItem/ClaimSplitter.cs
@@ -23,21 +23,70 @@ public sealed class ClaimSplitter
         - Aufzaehlungen ('A, B und C') in mehrere Claims trennen, WENN A/B/C unabhaengig pruefbar sind.
         - Eine Modalitaet/Bedingung, die zum Kern gehoert ('nur', 'ausschliesslich', 'im MVP', 'spaeter'),
           BLEIBT im jeweiligen atomaren Claim erhalten — sie ist pruef-relevant, nicht abtrennen.
+        - Erhalte pruef-relevante Facetten explizit: Komponente/Oberflaeche, Akteur/Nutzer, Aktion, Objekt,
+          Modalitaet, Status und Scope. Diese Facetten duerfen nicht still verschwinden.
+        - Du darfst den atomaren Claim minimal semantisch normalisieren, damit er gegen Transkript-Evidence
+          pruefbar ist. Beispiel: "Frontend zeigt Bestellungen an" kann als "Kunden koennen Bestellungen im
+          Kundenportal sehen" normalisiert werden, ABER die Facette component="Frontend/Kundenportal" muss
+          erhalten bleiben, damit sie spaeter separat geprueft werden kann.
         - Ist die Unit bereits atomar: gib sie UNVERAENDERT als einen Claim zurueck.
         - Keine Aussage erfinden, keine weglassen, nicht umdeuten. Reines Zerlegen.
         - Pro Claim ein vollstaendiger, fuer sich verstaendlicher Satz (keine blossen Stichworte).
 
         Antworte ausschliesslich mit JSON:
-        { "claims": ["...", "..."] }
+        {
+          "atomicClaims": [
+            {
+              "artifactQuote": "Originalaussage oder relevanter Ausschnitt",
+              "normalizedClaim": "minimal normalisierter atomarer Claim",
+              "facets": {
+                "component": "Komponente/Oberflaeche oder null",
+                "actor": "Akteur/Nutzer oder null",
+                "action": "Aktion oder null",
+                "object": "Objekt oder null",
+                "modality": "Modalitaet/Bedingung oder null",
+                "status": "Status/Entscheidungsstand oder null",
+                "scope": "Scope/Zeitraum/Phase oder null"
+              },
+              "splitReason": "kurz"
+            }
+          ]
+        }
         """;
 
     private const string SchemaJson = """
         {
           "type": "object",
           "properties": {
-            "claims": { "type": "array", "items": { "type": "string" } }
+            "atomicClaims": {
+              "type": "array",
+              "items": {
+                "type": "object",
+                "properties": {
+                  "artifactQuote": { "type": "string" },
+                  "normalizedClaim": { "type": "string" },
+                  "facets": {
+                    "type": "object",
+                    "properties": {
+                      "component": { "type": ["string", "null"] },
+                      "actor": { "type": ["string", "null"] },
+                      "action": { "type": ["string", "null"] },
+                      "object": { "type": ["string", "null"] },
+                      "modality": { "type": ["string", "null"] },
+                      "status": { "type": ["string", "null"] },
+                      "scope": { "type": ["string", "null"] }
+                    },
+                    "required": ["component", "actor", "action", "object", "modality", "status", "scope"],
+                    "additionalProperties": false
+                  },
+                  "splitReason": { "type": "string" }
+                },
+                "required": ["artifactQuote", "normalizedClaim", "facets", "splitReason"],
+                "additionalProperties": false
+              }
+            }
           },
-          "required": ["claims"],
+          "required": ["atomicClaims"],
           "additionalProperties": false
         }
         """;
@@ -57,6 +106,12 @@ public sealed class ClaimSplitter
     }
 
     public async Task<IReadOnlyList<string>> SplitAsync(string unit, string artifactType, CancellationToken ct)
+    {
+        var claims = await SplitDetailedAsync(unit, artifactType, ct).ConfigureAwait(false);
+        return claims.Select(c => c.NormalizedClaim).ToList();
+    }
+
+    public async Task<IReadOnlyList<AtomicClaim>> SplitDetailedAsync(string unit, string artifactType, CancellationToken ct)
     {
         var options = new ChatOptions { Temperature = 0.0f };
         if (_structuredOutput) options.ResponseFormat = ResponseFormat;
@@ -66,23 +121,38 @@ public sealed class ClaimSplitter
              new ChatMessage(ChatRole.User, $"ARTEFAKTTYP: {artifactType}\n\nUNIT:\n{unit}")],
             options, ct).ConfigureAwait(false);
 
-        var claims = ParseClaims(response.Text);
+        var claims = ParseAtomicClaims(response.Text);
         // Fallback: unzerlegbar/Parsefehler → die Unit selbst als ein Claim (kein stiller Verlust).
-        return claims.Count > 0 ? claims : new[] { unit.Trim() };
+        return claims.Count > 0 ? claims : new[] { AtomicClaim.FromUnit(unit) };
     }
 
     /// <summary>Deterministisches Parsen (testbar ohne LLM). Leere/leerzeilige Claims verworfen.</summary>
     public static IReadOnlyList<string> ParseClaims(string? text)
+        => ParseAtomicClaims(text).Select(c => c.NormalizedClaim).ToList();
+
+    public static IReadOnlyList<AtomicClaim> ParseAtomicClaims(string? text)
     {
         var json = ExtractJson(text);
-        if (json is null) return Array.Empty<string>();
+        if (json is null) return Array.Empty<AtomicClaim>();
         try
         {
             var p = JsonSerializer.Deserialize<Envelope>(json, Json);
-            if (p?.Claims is null) return Array.Empty<string>();
-            return p.Claims.Where(c => !string.IsNullOrWhiteSpace(c)).Select(c => c.Trim()).ToList();
+            if (p?.AtomicClaims is not null)
+            {
+                return p.AtomicClaims
+                    .Where(c => !string.IsNullOrWhiteSpace(c.NormalizedClaim))
+                    .Select(c => c.Trimmed())
+                    .ToList();
+            }
+
+            // Backward-compatible parser for old spike payloads/tests: { "claims": ["..."] }.
+            if (p?.Claims is null) return Array.Empty<AtomicClaim>();
+            return p.Claims
+                .Where(c => !string.IsNullOrWhiteSpace(c))
+                .Select(c => AtomicClaim.FromClaim(c))
+                .ToList();
         }
-        catch (JsonException) { return Array.Empty<string>(); }
+        catch (JsonException) { return Array.Empty<AtomicClaim>(); }
     }
 
     private static string? ExtractJson(string? text)
@@ -93,5 +163,72 @@ public sealed class ClaimSplitter
         return s < 0 || e <= s ? null : text.Substring(s, e - s + 1);
     }
 
-    private sealed record Envelope([property: JsonPropertyName("claims")] List<string>? Claims);
+    private sealed record Envelope(
+        [property: JsonPropertyName("atomicClaims")] List<AtomicClaim>? AtomicClaims,
+        [property: JsonPropertyName("claims")] List<string>? Claims);
+}
+
+public sealed record AtomicClaim(
+    [property: JsonPropertyName("artifactQuote")] string ArtifactQuote,
+    [property: JsonPropertyName("normalizedClaim")] string NormalizedClaim,
+    [property: JsonPropertyName("facets")] ClaimFacets Facets,
+    [property: JsonPropertyName("splitReason")] string SplitReason)
+{
+    public static AtomicClaim FromUnit(string unit)
+        => new(unit.Trim(), unit.Trim(), ClaimFacets.Empty, "fallback: unit unveraendert");
+
+    public static AtomicClaim FromClaim(string claim)
+        => new(claim.Trim(), claim.Trim(), ClaimFacets.Empty, "legacy claims-array");
+
+    public AtomicClaim Trimmed()
+        => this with
+        {
+            ArtifactQuote = ArtifactQuote.Trim(),
+            NormalizedClaim = NormalizedClaim.Trim(),
+            SplitReason = SplitReason.Trim(),
+            Facets = Facets.Trimmed()
+        };
+}
+
+public sealed record ClaimFacets(
+    [property: JsonPropertyName("component")] string? Component,
+    [property: JsonPropertyName("actor")] string? Actor,
+    [property: JsonPropertyName("action")] string? Action,
+    [property: JsonPropertyName("object")] string? Object,
+    [property: JsonPropertyName("modality")] string? Modality,
+    [property: JsonPropertyName("status")] string? Status,
+    [property: JsonPropertyName("scope")] string? Scope)
+{
+    public static ClaimFacets Empty { get; } = new(null, null, null, null, null, null, null);
+
+    public ClaimFacets Trimmed()
+        => new(
+            Normalize(Component),
+            Normalize(Actor),
+            Normalize(Action),
+            Normalize(Object),
+            Normalize(Modality),
+            Normalize(Status),
+            Normalize(Scope));
+
+    public IReadOnlyDictionary<string, string> ToDictionary()
+    {
+        var map = new Dictionary<string, string>(StringComparer.Ordinal);
+        Add(map, "component", Component);
+        Add(map, "actor", Actor);
+        Add(map, "action", Action);
+        Add(map, "object", Object);
+        Add(map, "modality", Modality);
+        Add(map, "status", Status);
+        Add(map, "scope", Scope);
+        return map;
+    }
+
+    private static string? Normalize(string? value)
+        => string.IsNullOrWhiteSpace(value) ? null : value.Trim();
+
+    private static void Add(Dictionary<string, string> map, string key, string? value)
+    {
+        if (!string.IsNullOrWhiteSpace(value)) map[key] = value.Trim();
+    }
 }
diff --git a/AgenticSdlc.Host/Program.cs b/AgenticSdlc.Host/Program.cs
index ca41771..b636cbe 100644
--- a/AgenticSdlc.Host/Program.cs
+++ b/AgenticSdlc.Host/Program.cs
@@ -129,6 +129,105 @@ if (args.Length > 0 && string.Equals(args[0], "claim-split-spike", StringCompari
     return;
 }
 
+// Coverage-Spike: source-native Claims gegen Artefakt prüfen (Quelle -> Artefakt), isoliert.
+if (args.Length > 0 && string.Equals(args[0], "source-claim-coverage-spike", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.SourceClaimCoverageSpikeRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Coverage-Matrix-Spike: kuratierte SourceClaims gegen alle Artefakte pruefen; not_applicable bleibt sichtbar.
+if (args.Length > 0 && string.Equals(args[0], "source-claim-coverage-matrix", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.SourceClaimCoverageMatrixRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Coverage-Matrix-v2-Spike: Applicability und Coverage getrennt gegen adjudizierte Matrix pruefen.
+if (args.Length > 0 && string.Equals(args[0], "source-claim-coverage-matrix-v2", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.SourceClaimCoverageMatrixV2Runner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Coverage-Matrix-Batch-Spike: alle SourceClaims gegen ein Artefakt in einem Call pruefen.
+if (args.Length > 0 && string.Equals(args[0], "source-claim-coverage-matrix-batch", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.SourceClaimCoverageMatrixBatchRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Evidence-first Semantic-Ledger-Spike: bestaetigte Quellsemantik -> traceable Artefakt -> lokale Verifikation.
+if (args.Length > 0 && string.Equals(args[0], "evidence-first-spike", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.EvidenceFirstSpikeRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Human-Artefakt-Spike: evidence-first claims.json -> lesbares Markdown mit sichtbaren SourceClaim-Refs.
+if (args.Length > 0 && string.Equals(args[0], "human-artifact-spike", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.HumanArtifactSpikeRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Human-Artefakt-Projektion: claims.json -> Markdown entlang sichtbarer SourceClaim-Refs deterministisch pruefen.
+if (args.Length > 0 && string.Equals(args[0], "human-artifact-projection", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.HumanArtifactProjectionRunner.RunAsync(args, repoRoot);
+    return;
+}
+
+// Semantic-Ledger-Extraction-Spike: Transkript -> facettierter Ledger -> Recall gegen bestaetigte Fixture.
+if (args.Length > 0 && string.Equals(args[0], "semantic-ledger-extraction-spike", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.SemanticLedgerExtractionSpikeRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Semantic-Ledger-Extract (NUR Extraktion+Canonicalization, keine Fixture/kein Match):
+// Kandidaten-Ledger fuer ein NEUES Transkript erzeugen, aus dem dann manuell eine Fixture bestaetigt wird.
+if (args.Length > 0 && string.Equals(args[0], "semantic-ledger-extract", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.SemanticLedgerExtractRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Coverage-Spike Stufe 2: Transcript -> auto SourceClaims -> Recall + E2E Coverage gegen Fixture, isoliert.
+if (args.Length > 0 && string.Equals(args[0], "source-claim-extraction-spike", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.SourceClaimExtractionSpikeRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Coverage-Spike Stufe 3: Transcript einmal -> globaler SourceClaim-Ledger -> Recall + E2E Coverage, isoliert.
+if (args.Length > 0 && string.Equals(args[0], "source-claim-ledger-spike", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.GlobalSourceClaimExtractionSpikeRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Coverage-Spike Stufe 4: Global SourceClaim Ledger -> ArtifactObligation -> Recall/Kandidatenreduktion, isoliert.
+if (args.Length > 0 && string.Equals(args[0], "source-claim-obligation-spike", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.ArtifactObligationSpikeRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Coverage-Spike Stufe 5: Transcript einmal -> artefaktspezifische SourceObligations -> Recall, isoliert.
+if (args.Length > 0 && string.Equals(args[0], "source-obligation-extraction-spike", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.SourceObligationExtractionSpikeRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
+// Coverage-Spike Stufe 6: Global SourceClaim Ledger -> konservative Selection -> Recall/Kandidatenreduktion.
+if (args.Length > 0 && string.Equals(args[0], "source-claim-selection-spike", StringComparison.OrdinalIgnoreCase))
+{
+    Environment.ExitCode = await AgenticSdlc.Host.Phases.Phase2.Evaluation.PerItem.SourceClaimSelectionSpikeRunner.RunAsync(args, settings, repoRoot);
+    return;
+}
+
 // C1: zusammengesetzter Review (Grounding + Coverage) → EIN ReviewResult pro Artefakt (offline, ReviewService).
 if (args.Length > 0 && string.Equals(args[0], "review", StringComparison.OrdinalIgnoreCase))
 {
diff --git a/AgenticSdlc.Tests/Review/ClaimSplitterTests.cs b/AgenticSdlc.Tests/Review/ClaimSplitterTests.cs
index a995d56..3fa73eb 100644
--- a/AgenticSdlc.Tests/Review/ClaimSplitterTests.cs
+++ b/AgenticSdlc.Tests/Review/ClaimSplitterTests.cs
@@ -40,4 +40,64 @@ public sealed class ClaimSplitterTests
         Assert.Single(claims);
         Assert.Equal("Login per E-Mail.", claims[0]);
     }
+
+    [Fact]
+    public void ParseAtomicClaims_Reads_Normalized_Claim_And_Facets()
+    {
+        var text = """
+            {
+              "atomicClaims": [
+                {
+                  "artifactQuote": "Das Kundenportal-Frontend zeigt Bestellungen an.",
+                  "normalizedClaim": "Kunden koennen Bestellungen im Kundenportal sehen.",
+                  "facets": {
+                    "component": "Kundenportal-Frontend",
+                    "actor": "Kunden",
+                    "action": "sehen",
+                    "object": "Bestellungen",
+                    "modality": null,
+                    "status": null,
+                    "scope": "Kundenportal"
+                  },
+                  "splitReason": "Objekt aus Aufzaehlung isoliert, Oberflaeche erhalten."
+                }
+              ]
+            }
+            """;
+
+        var claims = ClaimSplitter.ParseAtomicClaims(text);
+
+        Assert.Single(claims);
+        Assert.Equal("Kunden koennen Bestellungen im Kundenportal sehen.", claims[0].NormalizedClaim);
+        Assert.Equal("Kundenportal-Frontend", claims[0].Facets.Component);
+        Assert.Equal("Bestellungen", claims[0].Facets.Object);
+    }
+
+    [Fact]
+    public void ParseClaims_Uses_Normalized_Claim_For_New_Format()
+    {
+        var claims = ClaimSplitter.ParseClaims("""
+            {
+              "atomicClaims": [
+                {
+                  "artifactQuote": "Frontend zeigt Rechnungen an.",
+                  "normalizedClaim": "Kunden koennen Rechnungen im Kundenportal abrufen.",
+                  "facets": {
+                    "component": "Frontend",
+                    "actor": "Kunden",
+                    "action": "abrufen",
+                    "object": "Rechnungen",
+                    "modality": null,
+                    "status": null,
+                    "scope": null
+                  },
+                  "splitReason": "Download/Anzeige normalisiert, UI-Facette erhalten."
+                }
+              ]
+            }
+            """);
+
+        Assert.Single(claims);
+        Assert.Equal("Kunden koennen Rechnungen im Kundenportal abrufen.", claims[0]);
+    }
 }
diff --git a/docs/architecture.md b/docs/architecture.md
deleted file mode 100644
index 89373f6..0000000
Binary files a/docs/architecture.md and /dev/null differ
diff --git a/docs/open-questions.md b/docs/open-questions.md
deleted file mode 100644
index cd69df5..0000000
--- a/docs/open-questions.md
+++ /dev/null
@@ -1,36 +0,0 @@
-# Offene Fragen und Klärungsbedarfe der Kommunikations-App
-
-## 1. Fachliche offene Fragen
-
-- Wie werden individuelle Kommunikationsweisen der Bewohner standardisiert und dokumentiert, um eine verständliche Nutzung für Betreuer und Angehörige sicherzustellen?
-- Welche konkreten Zugriffsrechte gelten für Bewohnerprofile, insbesondere im Hinblick auf Datenschutz und eingeschränkte Einsichtsmöglichkeiten?
-- Wie soll die Balance zwischen Funktionsumfang und Bedienbarkeit konkret erreicht und gemessen werden? Welche Funktionen haben Priorität?
-
-## 2. Technische offene Fragen
-
-- Wie wird die Datenbankarchitektur konkret gestaltet hinsichtlich lokaler Speicherung, Synchronisation und Performance?
-- Welche Technologie und Architektur kommen für die lokale Synchronisation zum Einsatz?
-- Welche technischen Datenschutzmaßnahmen und Sicherheitsmechanismen werden implementiert, um differenzierte Zugriffsrechte sicherzustellen?
-- Gibt es bekannte technische Einschränkungen oder Herausforderungen aufgrund der plattformübergreifenden Flutter/Dart-Entwicklung?
-
-## 3. Widersprüche und Unklarheiten
-
-- Wie wird die Abstimmung mit dem separaten Datenschutzprojekt organisiert, um die Auswirkungen auf technische Architektur und Rechteverwaltung zu klären?
-- Wie wird die Nutzerverwaltung und Zugangsbeschränkung technisch und organisatorisch umgesetzt, um die fehlende freie Registrierung sicherzustellen?
-- Welche konkreten Kriterien und Maßnahmen gewährleisen eine ausgewogene Balance von Funktionsumfang und Benutzbarkeit?
-
-## 4. Fehlende Informationen
-
-- Detailinformationen zur Datenbank- und Synchronisationslösung (z.B. Datenbanktyp, Offline-Funktion, Synchronisationsprotokoll).
-- Datenschutzrichtlinien und verantwortliche Stellen im Datenschutzprojekt.
-- Detaillierte Beschreibung der Filter- und Suchfunktionen für Profile und Kommunikationsseiten.
-- Konkrete Konzepte zur Nutzer- und Rechteverwaltung mit Beispielen zu Rollenzuweisungen und Berechtigungen.
-
-## 5. Mögliche Ansprechpartner/Rollen
-
-- Datenschutzbeauftragter / Datenschutzprojektteam
-- Technische Architekten / Entwickler für Datenbank und Synchronisation
-- UX-/Usability-Experten
-- Fachpersonal der betreuten Einrichtung (Gesamtleiter, Heilerziehungspfleger, soziales Fachpersonal)
-
-Dieser Fragekatalog soll dazu beitragen, die noch offenen Unsicherheiten im Projekt gezielt zu adressieren und die weitere Entwicklung sicher und abgestimmt voranzutreiben.
diff --git a/docs/requirements.md b/docs/requirements.md
deleted file mode 100644
index 8ff1307..0000000
--- a/docs/requirements.md
+++ /dev/null
@@ -1,36 +0,0 @@
-## Functional Requirements
-
-- Die App muss es erm46glichen, individuelle Kommunikationsweisen der Bewohner zu dokumentieren und nachzuschlagen.
-- Es muss Bereiche geben f5Cr "About Me" (pers5C6nliche Infos, Bilder, Hobbys), "Kommunikation" (verbal und nonverbal mit visuellen/bildlichen Unterst5Ctzungen), "Video" (Videos zu spezifischen Kommunikationssituationen), "Kalender & Medikamentenverwaltung" sowie eine "No-Go-Liste" (Tabus und Empfindlichkeiten).
-- Es muss eine Nutzer- und Rechteverwaltung geben mit mindestens den Rollen: Admin, Betreuer, Angeh544rige und Bewohner (mit eingeschr52nkten Zugriffsrechten).
-- Die App muss dynamisches Hinzuf56gen und Erweitern von Inhalten in allen Bereichen erlauben.
-- Die App muss Such- und Filterfunktionen f5Cr Profil5Cbersichten und Kommunikationsseiten bereitstellen.
-- Die App muss plattform5Cbergreifend (Android und iOS) mit Flutter/Dart entwickelt werden.
-- Die App muss exklusiven Zugang 56ber Accounts erm56glichen, eine freie Registrierung ist nicht erlaubt.
-- Die Einsicht in die Daten muss differenziert je nach Einrichtung geregelt werden.
-
-## Non-functional Requirements
-
-- Die App muss eine benutzerfreundliche Bedienbarkeit gew5C4hrleisten und die Balance zwischen Funktionsumfang und Usability sorgf5C4ltig beachten.
-- Die App muss Datenschutzprinzipien beachten und den Zugriff entsprechend sicher gestalten.
-- Die App muss eine performante Datenbank- und Synchronisationsl56sung integrieren (Details noch offen).
-
-## Constraints/Compliance
-
-- Die App muss den datenschutzrechtlichen Vorgaben entsprechen (konkrete Ma56nahmen noch zu kl56ren in gesondertem Projekt).
-- Die Plattform ist auf Flutter/Dart beschr5Een mit Android- und iOS-Unterst5CAtzung.
-- Es gibt keine freie Nutzerregistrierung, Zugriffe sind nur 5Eber Accounts innerhalb der Einrichtung m56glich.
-
-## Assumptions and Open Points
-
-- Die genaue technische Ausgestaltung der Datenbank und der lokalen Synchronisation ist noch offen und muss im weiteren Projektverlauf definiert werden.
-- Die Details zur Einhaltung der Datenschutz- und Rechtsvorgaben sind noch unklar und Gegenstand eines separaten Projektes.
-- Die Balance zwischen Funktionsumfang und Bedienbarkeit wird noch abgestimmt, um 5Cberfrachtung der App zu vermeiden.
-- Es wird angenommen, dass die Nutzer (Betreuer, Angeh544rige, Bewohner) mit den Rollen und Rechteeinschr54nkungen technisch klar abgebildet werden k56nnen.
-
-## Traceability
-
-- Anforderungen basieren auf Interviews mit Gesamtleiter, Fachpersonal, Heilerziehungspflegern und Studierenden (Transkript "Interview-Einrichtung.txt" vom 23.06.2026).
-- App-Bereiche und Nutzerrollen sind aus den fachlichen Anforderungen und funktionalen Beschreibungen abgeleitet.
-- Datenschutzaspekte und technische Unsicherheiten wurden explizit als offene Punkte im Projektkontext genannt.
-- Die Technologieentscheidung Flutter/Dart basiert auf Projektkontextangaben zur plattform5Cbergreifenden Entwicklung.
\ No newline at end of file
diff --git a/docs/risks.md b/docs/risks.md
deleted file mode 100644
index 0a6e4c3..0000000
--- a/docs/risks.md
+++ /dev/null
@@ -1,41 +0,0 @@
-# Risikoanalyse für die Kommunikations-App
-
-## Fachliche Risiken
-
-- **Balance zwischen Funktionsumfang und Bedienbarkeit**: Die Anforderungen umfassen viele Funktionen, die App soll aber benutzerfreundlich und übersichtlich bleiben. Eine Überfrachtung kann die Akzeptanz und Nutzbarkeit beeinträchtigen.
-- **Nutzerrollen und Rechteverwaltung**: Die klare und sichere Abbildung komplexer Rollen (Admin, Betreuer, Angehörige, Bewohner mit eingeschränkten Zugriffsrechten) kann fehleranfällig sein und zu ungewolltem Datenzugriff oder -verlust führen.
-
-## Technische Risiken
-
-- **Offene technische Architektur der Datenbank und Synchronisation**: Ohne eine frühzeitige, konkrete technische Ausgestaltung besteht das Risiko von Implementierungsverzögerungen, Performanceproblemen oder inkonsistenten Daten.
-- **Plattformübergreifende Entwicklung mit Flutter/Dart**: Flutter ist mächtig, aber es können technische Einschränkungen oder Kompatibilitätsprobleme auftreten, die den Entwicklungsaufwand erhöhen.
-
-## Compliance- und Datenschutzrisiken
-
-- **Ungeklärte Datenschutz- und Rechtsvorgaben**: Da der Datenschutz ein gesondertes Projekt ist und Details noch offen sind, besteht das Risiko, dass gesetzliche Anforderungen nicht vollständig eingehalten werden, was rechtliche Konsequenzen nach sich ziehen kann.
-- **Keine freie Registrierung, nur Zugang über Accounts der Einrichtung**: Dies muss technisch und organisatorisch sicher umgesetzt werden, um unerlaubte Zugriffe zu verhindern.
-
-## Widersprüche und Unsicherheiten
-
-- Die genaue Ausgestaltung der technischen Basis (Datenbank, Synchronisation) ist offen und birgt Unsicherheiten.
-- Die Datenschutzaspekte und -maßnahmen sind noch nicht definiert.
-- Es ist unklar, wie die Balance zwischen Funktionsumfang und Bedienbarkeit optimal erreicht wird.
-
-## Mögliche Auswirkungen
-
-- Verzögerungen im Projektzeitplan
-- Akzeptanzprobleme bei den Nutzern durch schlechte Usability oder unklare Rechtevergabe
-- Rechtliche Probleme und Datenschutzverstöße
-- Performance- und Stabilitätsprobleme der App
-
-## Mögliche Gegenmaßnahmen und Klärungsbedarfe
-
-- Frühzeitige und enge Abstimmung mit dem Datenschutzprojekt, um Anforderungen und Maßnahmen zu klären.
-- Definition und Prototypen der technischen Architektur für Datenbank und Synchronisation vor Implementierungsstart.
-- Iterative Usability-Tests zur Sicherstellung einer angemessenen Bedienbarkeit trotz Funktionsumfang.
-- Klare und sichere Implementierung der Nutzer- und Rechteverwaltung mit Fokus auf Datenschutz und Benutzerfreundlichkeit.
-- Risiko- und Change-Management etablieren, um auf offene Punkte flexibel reagieren zu können.
-
----
-
-*Dieses Risikoartefakt basiert auf dem bereitgestellten Projektkontext und den Requirements (Stand 23.06.2026).*
\ No newline at end of file
diff --git a/input/transcripts/T9999_chaos.txt b/input/transcripts/T9999_chaos.txt
deleted file mode 100644
index dfd1946..0000000
--- a/input/transcripts/T9999_chaos.txt
+++ /dev/null
@@ -1,821 +0,0 @@
-[Anna]
-Also wir brauchen definitiv ein Kundenportal. Aber nicht nur ein Portal, eher so eine Plattform. Also... vielleicht doch erstmal nur ein Portal. Web first. Mobile später. Oder andersrum? Ich weiß nicht, Sales will eigentlich mobil arbeiten.
-
-[Ben]
-Moment, mobile heißt native App oder responsive Web? Weil native wäre teuer. Und wir haben eigentlich kein Budget für zwei Frontends. Und ehrlich gesagt ist unser Backend noch nicht mal API-ready.
-
-[Clara]
-Bevor wir über Frontends reden: DSGVO. Wir speichern Kundendaten. Login per E-Mail? Dann brauchen wir Double-Opt-In. Und Logging. Und Löschkonzepte. Und Auftragsverarbeitungsverträge.
-
-[Anna]
-Ja aber das darf uns jetzt nicht blockieren. MVP in 8 Wochen wurde versprochen. Also erstmal Login per E-Mail und Passwort. SSO wäre schön. Vielleicht Azure AD. Oder Google? Oder beides?
-
-[Ben]
-Moment, SSO heißt Identity Provider Integration. Wir haben kein zentrales IAM. SAP hat irgendwas, aber das ist uralt.
-
-[Clara]
-SAP? Wenn SAP involviert ist, dann reden wir von personenbezogenen Daten im ERP. Dann brauchen wir Audit Trails. Und Zugriffskontrolle. Und Rollenmodelle.
-
-[Anna]
-Ja Rollen. Admin und normaler User. Vielleicht noch Manager. Und Support. Oder brauchen wir Support? Wer pflegt eigentlich Stammdaten?
-
-[Ben]
-Stammdaten liegen in SAP. Aber die sind teilweise unvollständig. Also wenn wir Angebote generieren wollen – was Sales unbedingt will – dann brauchen wir Produktdaten, Preise, Rabattlogik.
-
-[Anna]
-Ja genau! Angebote schneller erstellen. Das ist eigentlich das Hauptziel. Portal ist nur Mittel zum Zweck.
-
-[Clara]
-Moment, ich dachte es geht um Rechnungsdownload?
-
-[Anna]
-Ja auch. Also Kunden sollen Bestellungen sehen und Rechnungen downloaden. Aber Sales will Angebote erstellen. Und Marketing will Push Notifications.
-
-[Ben]
-Push Notifications? Das hatten wir nie besprochen.
-
-[Anna]
-Doch, im letzten Meeting. Also ich glaube zumindest.
-
-[Clara]
-Push heißt Einwilligung. Wieder DSGVO. Und Tracking ist auch ein Thema.
-
-[Ben]
-Tracking? Wir haben keine Analytics Infrastruktur.
-
-[Anna]
-Brauchen wir aber vielleicht später. Also nicht im MVP. Oder doch? Wir müssen KPIs messen.
-
-[Ben]
-Was sind die KPIs?
-
-[Anna]
-Conversion Rate. Angebot → Bestellung. Und Zeit bis Angebot.
-
-[Clara]
-Zeit bis Angebot ist kein personenbezogenes Datum. Das ist gut.
-
-[Ben]
-Aber Angebot selbst enthält personenbezogene Daten.
-
-[Anna]
-Also brauchen wir Verschlüsselung. Ende-zu-Ende? Oder reicht TLS?
-
-[Ben]
-TLS reicht. E2E ist unrealistisch.
-
-[Clara]
-Auditierbarkeit ist Pflicht. Wer hat wann was geändert?
-
-[Anna]
-Ja Logging. Aber bitte nicht zu komplex. Wir haben keine Zeit.
-
-[Ben]
-Security Review dauert 6 Wochen. Das sprengt die 8 Wochen MVP.
-
-[Anna]
-Dann machen wir es ohne Security Review?
-
-[Clara]
-Das ist keine Option.
-
-[Ben]
-Vielleicht können wir Managed Services nehmen. Kein neuer DB Server – IT hat das klar gesagt.
-
-[Anna]
-Ja kein neuer DB Server. Managed Services sind okay. Aber bitte günstig.
-
-[Ben]
-Günstig und skalierbar widerspricht sich.
-
-[Clara]
-Und wo werden Daten gehostet? EU only?
-
-[Anna]
-Ja EU only. Oder zumindest DSGVO-konform.
-
-[Ben]
-Das ist nicht dasselbe.
-
-[Clara]
-Genau.
-
-[Anna]
-Okay also:
-- Kundenportal
-- Angebote
-- Rechnungen
-- Login
-- Rollen
-- Logging
-- SAP Integration
-- Keine neue DB
-- MVP in 8 Wochen
-- Security Review irgendwie
-- DSGVO
-- KPIs
-- Mobile vielleicht
-- SSO optional
-- API Layer?
-
-[Ben]
-Wir brauchen definitiv eine API Layer. Sonst können wir nichts integrieren.
-
-[Clara]
-API muss abgesichert sein. OAuth? Oder API Keys?
-
-[Ben]
-OAuth wäre besser. Aber komplexer.
-
-[Anna]
-Komplexer heißt mehr Zeit.
-
-[Ben]
-Ja.
-
-[Clara]
-Und wir haben noch gar nicht über Backup gesprochen.
-
-[Anna]
-Backup?
-
-[Ben]
-Ja Disaster Recovery.
-
-[Anna]
-Brauchen wir das für MVP?
-
-[Clara]
-Wenn Kundendaten drin sind: ja.
-
-[Anna]
-Okay also doch.
-
-[Ben]
-Und Performance. Wie viele User erwarten wir?
-
-[Anna]
-Weiß ich nicht. Vielleicht 200. Vielleicht 2000. Vielleicht 20.000 wenn es gut läuft.
-
-[Ben]
-Das ist ein Unterschied.
-
-[Clara]
-Skalierbarkeit muss berücksichtigt werden.
-
-[Anna]
-Aber bitte kein Overengineering.
-
-[Ben]
-Overengineering und skalierbar sind ein Spannungsfeld.
-
-[Anna]
-Wir drehen uns im Kreis.
-
-[Clara]
-Und wir haben noch keine klare Zieldefinition.
-
-[Anna]
-Ziel ist schneller Angebote erstellen.
-
-[Ben]
-Und Portal.
-
-[Clara]
-Und Compliance.
-
-[Anna]
-Ja.
-
-(Stille)
-
-[Ben]
-Was ist mit Support? Ticketsystem?
-
-[Anna]
-Nein erstmal nicht.
-
-[Clara]
-Aber wenn ein Kunde seine Daten löschen will?
-
-[Anna]
-Dann muss das gehen.
-
-[Ben]
-Also brauchen wir ein Löschkonzept.
-
-[Clara]
-Und ein Rollen- und Berechtigungskonzept.
-
-[Anna]
-Wer macht das?
-
-[Ben]
-Wir brauchen einen Architekten.
-
-[Anna]
-Wir haben keinen.
-
-[Clara]
-Dann müssen wir das sauber dokumentieren.
-
-[Anna]
-Dokumentation ist wichtig, aber bitte nicht zu viel.
-
-[Ben]
-Ohne Dokumentation kein Security Review.
-
-[Anna]
-Okay dann doch.
-
-[David]
-Sorry, ich komme zu spät dazu. Ich bin von Customer Support. Ich habe gehört, es soll kein Ticketsystem geben. Das ist aus Support-Sicht schwierig, weil Kunden uns heute wegen Rechnungen, Lieferstatus und falschen Rabatten ständig schreiben.
-
-[Anna]
-Ticketsystem war nicht im MVP. Oder vielleicht nur Kontaktformular? Aber kein richtiges Ticketsystem.
-
-[David]
-Wenn es nur ein Kontaktformular ist, brauchen wir trotzdem eine Zuordnung zum Kundenkonto, sonst können wir die Anfrage nicht bearbeiten.
-
-[Clara]
-Wenn Support-Anfragen personenbezogene Daten enthalten, dann müssen sie in das Löschkonzept und in die Auskunftspflicht einbezogen werden.
-
-[Ben]
-Und technisch heißt das wieder Persistenz. Wir wollten keine neue Datenbank.
-
-[Anna]
-Managed Service war okay.
-
-[Ben]
-Ja, aber wir haben noch nicht entschieden, welcher. Und SAP kann keine Support-Tickets speichern.
-
-[David]
-Ich will nur verhindern, dass wir nach dem Launch manuell Excel-Listen pflegen.
-
-[Anna]
-Excel ist keine Option.
-
-[Clara]
-Excel mit personenbezogenen Kundendaten wäre aus Compliance-Sicht problematisch.
-
-[Eva]
-Ich bin aus Finance. Mir fehlt ein Punkt: Angebote müssen freigegeben werden, wenn der Rabatt über 15 Prozent liegt. Manche Kunden haben Sonderkonditionen, aber nicht jeder Sales-Mitarbeiter darf die nutzen.
-
-[Anna]
-Das ist wichtig, aber vielleicht später.
-
-[Eva]
-Nein, ohne Freigabeprozess können falsche Angebote rausgehen. Das ist ein finanzielles Risiko.
-
-[Ben]
-Dann brauchen wir Workflow-Status für Angebote: draft, pending approval, approved, sent, accepted, rejected.
-
-[Anna]
-Aber nicht zu kompliziert.
-
-[Eva]
-Mindestens eine Freigabe durch Manager ab 15 Prozent Rabatt. Ab 30 Prozent vielleicht Finance.
-
-[Anna]
-Oder ab 20 Prozent. Ich bin mir nicht sicher.
-
-[Ben]
-Das muss klar definiert werden. Rabattlogik kommt aus SAP, aber Freigabeprozess wäre vermutlich im Portal.
-
-[Clara]
-Wer darf dann die Rabatte sehen? Das ist Rollen- und Berechtigungslogik.
-
-[David]
-Support sollte keine Preise ändern können, aber vielleicht Angebote einsehen dürfen, wenn Kunden anrufen.
-
-[Eva]
-Support darf Sonderkonditionen nicht sehen, wenn sie vertraulich sind.
-
-[Anna]
-Also Support sieht nur Status, nicht Details?
-
-[David]
-Dann kann Support nicht helfen.
-
-[Ben]
-Das ist ein klassischer Berechtigungskonflikt. Müssen wir modellieren.
-
-[Clara]
-Und protokollieren. Wer hat welches Angebot geöffnet?
-
-[Farid]
-Ich bin aus IT Operations. EU-only Hosting ist nicht einfach. Unser Standardanbieter repliziert Backups manchmal global, wenn man es nicht explizit deaktiviert.
-
-[Clara]
-Dann reicht "DSGVO-konform" nicht. Wir brauchen nachweisbare Datenresidenz.
-
-[Anna]
-Aber wenn EU-only teurer ist?
-
-[Farid]
-Ist es wahrscheinlich.
-
-[Eva]
-Wie viel teurer?
-
-[Farid]
-Kann ich noch nicht sagen.
-
-[Anna]
-Wir brauchen aber eine Kostenschätzung für den Vorstand bis Freitag.
-
-[Ben]
-Bis Freitag? Wir haben nicht mal eine Architekturentscheidung.
-
-[Anna]
-Dann schätzen wir grob.
-
-[Eva]
-Eine grobe Schätzung ohne Scope ist gefährlich.
-
-[Clara]
-Und ohne Datenschutzmaßnahmen unvollständig.
-
-[Ben]
-Und ohne SAP-Schnittstelle wertlos.
-
-[Anna]
-Okay, aber der Vorstand will Zahlen.
-
-[Farid]
-Noch ein Punkt: Wir haben eine interne Policy, dass neue externe Portale über das zentrale API Gateway laufen müssen.
-
-[Ben]
-Das habe ich vorhin API Layer genannt. Aber unser Gateway-Team hat gerade eine Warteliste von sechs Wochen.
-
-[Anna]
-Das passt nicht zum 8-Wochen-MVP.
-
-[Ben]
-Genau.
-
-[David]
-Kann man für MVP manuell Angebote hochladen?
-
-[Ben]
-Dann wäre es kein integriertes Portal.
-
-[Anna]
-Vielleicht als Zwischenlösung?
-
-[Eva]
-Manuelle Uploads erhöhen Fehlerwahrscheinlichkeit.
-
-[Clara]
-Und wer prüft, ob keine falschen Kundendaten hochgeladen werden?
-
-[Farid]
-Außerdem haben wir dann ein Berechtigungsproblem im File Storage.
-
-[Ben]
-Wir verschieben gerade vom Portal zu einem Dokumentenmanagementsystem.
-
-[Anna]
-Nein, Portal bleibt Ziel.
-
-[Clara]
-Was ist eigentlich mit Datenminimierung? Müssen alle Kundendaten im Portal liegen oder reicht ein Abruf aus SAP bei Bedarf?
-
-[Ben]
-Abruf aus SAP bei Bedarf wäre besser für Datenminimierung, aber schlechter für Performance und Verfügbarkeit.
-
-[Farid]
-Und SAP hat Wartungsfenster am Wochenende.
-
-[Anna]
-Kunden arbeiten auch am Wochenende.
-
-[David]
-Vor allem internationale Kunden.
-
-[Clara]
-International? Ich dachte nur DACH.
-
-[Anna]
-Zum Start DACH. Später EU. Vielleicht USA, wenn es gut läuft.
-
-[Clara]
-USA ändert Datenschutz und Vertragslage massiv.
-
-[Ben]
-Und Sprachen. Dann brauchen wir Internationalisierung.
-
-[Anna]
-Deutsch und Englisch sollten schon drin sein.
-
-[David]
-Support braucht mindestens Deutsch und Englisch.
-
-[Eva]
-Angebote müssen in der Währung des Kunden ausgestellt werden. EUR sicher, CHF vielleicht, USD später.
-
-[Ben]
-Mehrwährung ist nicht trivial, vor allem mit Rabattlogik und SAP.
-
-[Anna]
-CHF ist für Schweiz wichtig.
-
-[Clara]
-Schweiz ist nicht EU, aber hat eigene Datenschutzthemen.
-
-[Farid]
-Hosting in EU kann Schweizer Kunden bedienen, aber Vertragsprüfung nötig.
-
-[Ben]
-Wir brauchen Länder- und Währungsregeln.
-
-[Anna]
-Nicht im MVP?
-
-[Eva]
-Wenn Schweiz Pilotkunde ist, dann doch.
-
-[Anna]
-Pilotkunde ist noch nicht final.
-
-[David]
-Ich dachte Pilotkunde ist Müller AG aus der Schweiz.
-
-[Anna]
-Vielleicht. Oder Hansa GmbH in Deutschland. Das entscheidet Sales nächste Woche.
-
-[Ben]
-Das beeinflusst Scope massiv.
-
-[Clara]
-Und Datenschutzprüfung.
-
-[Eva]
-Und Währung.
-
-[Anna]
-Okay, notieren als offene Frage.
-
-[Ben]
-Noch ein technischer Punkt: SAP-Preise sind nicht immer in Echtzeit aktuell. Manche Rabatte werden nachts berechnet.
-
-[Anna]
-Aber Sales will sofort Angebote erstellen.
-
-[Eva]
-Wenn Preise nachts aktualisiert werden, darf ein Angebot tagsüber nicht mit falschen Rabatten rausgehen.
-
-[Ben]
-Dann brauchen wir Preisgültigkeit und vielleicht Warnhinweise.
-
-[Anna]
-Oder wir akzeptieren das Risiko.
-
-[Eva]
-Nein.
-
-[Clara]
-Wenn falsche Preise personenbezogene Kundendaten enthalten, ist das nicht primär Datenschutz, aber trotzdem Audit-relevant.
-
-[David]
-Kunden rufen dann Support an. Wieder Tickets.
-
-[Ben]
-Kreislauf.
-
-[Anna]
-Was ist mit PDF-Export von Angeboten?
-
-[Ben]
-Das wurde bisher nicht genannt.
-
-[Eva]
-Finance braucht PDF mit rechtlichen Fußnoten und Versionsnummer.
-
-[Clara]
-Und Datenschutzhinweise, wenn personenbezogene Daten enthalten sind.
-
-[Anna]
-PDF ist wichtig für Sales.
-
-[Ben]
-Dann haben wir Template-Management.
-
-[Farid]
-Templates müssen versioniert werden, sonst kann niemand nachvollziehen, welche Angebotsversion an welchen Kunden ging.
-
-[Eva]
-Genau. Und Angebote müssen nachträglich revisionssicher nachvollziehbar sein.
-
-[Clara]
-Revisionssicher heißt nicht automatisch unveränderbar, aber Änderungen müssen nachvollziehbar sein.
-
-[Ben]
-Dann brauchen wir Event-History oder Audit-Log pro Angebot.
-
-[Anna]
-Ist das MVP?
-
-[Eva]
-Für Freigabe und rechtliche Nachvollziehbarkeit: ja, zumindest minimal.
-
-[David]
-Kann der Kunde ein Angebot online akzeptieren?
-
-[Anna]
-Das wäre super.
-
-[Ben]
-Das ist ein Vertragsabschluss. Ganz anderes Thema.
-
-[Clara]
-Dann brauchen wir Nachweis der Zustimmung, Zeitstempel, eventuell AGB-Akzeptanz.
-
-[Eva]
-Und dann entsteht eine Bestellung. Das muss sauber nach SAP zurück.
-
-[Ben]
-SAP-Schreibzugriff hatten wir noch gar nicht entschieden. Vorhin war eher lesend.
-
-[Anna]
-Dann vielleicht nur Angebotsdownload, keine Online-Akzeptanz im MVP.
-
-[David]
-Aber ohne Online-Akzeptanz ist Conversion Tracking schlechter.
-
-[Anna]
-Stimmt.
-
-[Ben]
-Wir müssen entscheiden.
-
-[Clara]
-Und dokumentieren.
-
-[Farid]
-Noch ein Risiko: Unser zentrales Monitoring darf keine personenbezogenen Daten in Logs enthalten. Wenn wir alles auditieren, müssen wir zwischen Audit und technischem Logging unterscheiden.
-
-[Ben]
-Guter Punkt. Application logs, audit logs und security logs sind unterschiedliche Dinge.
-
-[Clara]
-Und unterschiedliche Aufbewahrungsfristen.
-
-[Anna]
-Wie lange müssen wir Angebote speichern?
-
-[Eva]
-Handelsrechtlich möglicherweise mehrere Jahre, aber das muss Legal prüfen.
-
-[Clara]
-Das kollidiert mit Löschanfragen. Recht auf Löschung ist nicht absolut, wenn gesetzliche Aufbewahrungspflichten bestehen.
-
-[Ben]
-Also brauchen wir Datenklassifikation und Retention Rules.
-
-[Anna]
-Das klingt nicht nach MVP.
-
-[Clara]
-Aber ohne Konzept riskieren wir Fehler.
-
-[David]
-Support braucht auch Einsicht in alte Angebote.
-
-[Eva]
-Finance auch.
-
-[Anna]
-Okay, minimaler Retention-Plan.
-
-[Ben]
-Ich sehe gerade: Wir sprechen von Kundenportal, Angebotsworkflow, Rechnungen, Support, Datenschutz, Analytics, Mehrwährung, PDF, SAP-Schreibzugriff und Monitoring. Das ist kein 8-Wochen-MVP.
-
-[Anna]
-Dann müssen wir schneiden. MVP:
-1. Login
-2. Angebote erstellen
-3. Rechnungen anzeigen
-4. SAP lesen
-5. Rollen minimal
-6. Audit minimal
-
-[Clara]
-Double-Opt-In und Löschung fehlen.
-
-[Eva]
-Freigabeprozess fehlt.
-
-[David]
-Support fehlt.
-
-[Farid]
-API Gateway und EU Hosting fehlen.
-
-[Ben]
-Backup fehlt.
-
-[Anna]
-Okay. Dann MVP:
-1. Login mit Double-Opt-In
-2. Angebotserstellung mit SAP-Lesedaten
-3. Rechnungsdownload
-4. Rollen Admin/Sales/Kunde
-5. Minimaler Audit Trail
-6. EU Managed Hosting
-7. Backup
-
-[Eva]
-Und Rabatt-Freigabe?
-
-[Anna]
-Vielleicht Phase 2.
-
-[Eva]
-Dann darf im MVP kein Rabatt über Standardsatz erstellt werden.
-
-[Ben]
-Das wäre eine Constraint: keine manuellen Sonderrabatte im MVP.
-
-[Anna]
-Gut.
-
-[David]
-Support?
-
-[Anna]
-Kontaktformular ohne Ticketpersistenz?
-
-[Clara]
-Dann werden Supportdaten per E-Mail verarbeitet. Auch Datenschutz.
-
-[Farid]
-E-Mail ist unstrukturiert und schlecht auditierbar.
-
-[Ben]
-Aber einfacher.
-
-[David]
-Support wird damit nicht glücklich.
-
-[Anna]
-Notieren als Risiko.
-
-[Clara]
-Bitte nicht "wir ignorieren es", sondern "bewusste Einschränkung".
-
-[Ben]
-Das ist wichtig für Scope.
-
-[Farid]
-Noch eins: Wir brauchen Umgebungen. Dev, Test, Prod. Testdaten dürfen keine echten Kundendaten enthalten.
-
-[Clara]
-Ja, Pseudonymisierung oder synthetische Testdaten.
-
-[Ben]
-SAP-Testsystem hat aber echte alte Kundendaten. Das ist ein Problem.
-
-[Clara]
-Dann dürfen die nicht ungeprüft verwendet werden.
-
-[Anna]
-Kann man das später klären?
-
-[Clara]
-Nein, wenn Entwicklung darauf zugreift, ist es sofort relevant.
-
-[Ben]
-Das blockiert Integrationstests.
-
-[Farid]
-Und CI/CD braucht Secrets Management.
-
-[Ben]
-Wir brauchen auch Rate Limits für API.
-
-[David]
-Kunden laden manchmal hunderte Rechnungen auf einmal herunter.
-
-[Ben]
-Dann brauchen wir Pagination und Download-Limits.
-
-[Anna]
-Aber bitte keine schlechte UX.
-
-[Eva]
-Massenexport von Rechnungen kann rechtlich relevant sein.
-
-[Clara]
-Und sicherheitsrelevant.
-
-[Ben]
-Wir brauchen Missbrauchserkennung.
-
-[Anna]
-Das ist zu viel.
-
-[Clara]
-Dann mindestens Rate Limiting und Audit.
-
-[Farid]
-Kann API Gateway übernehmen, wenn wir es bekommen.
-
-[Ben]
-Aber Gateway-Team hat sechs Wochen Warteliste.
-
-[Anna]
-Das ist wieder das Zeitproblem.
-
-[David]
-Was passiert, wenn SAP nicht erreichbar ist?
-
-[Ben]
-Angebote können dann nicht aktuell berechnet werden.
-
-[Eva]
-Dann darf kein Angebot erstellt werden, oder es muss als unverbindlich markiert sein.
-
-[Anna]
-Unverbindliche Angebote sind schlecht.
-
-[Ben]
-Dann brauchen wir Fallback oder Cache.
-
-[Clara]
-Cache enthält Kundendaten?
-
-[Ben]
-Teilweise Produkt- und Preisdaten, vielleicht kundenindividuelle Rabatte.
-
-[Clara]
-Dann wieder Datenschutz und Retention.
-
-[Anna]
-Können wir nur Produktdaten cachen, keine Kundendaten?
-
-[Ben]
-Vielleicht, aber Rabattlogik ist kundenspezifisch.
-
-[Eva]
-Dann ist Cache riskant.
-
-[Farid]
-Und Cache-Invalidierung ist aufwendig.
-
-[Ben]
-Bitte schreibt "SAP-Verfügbarkeit ist kritische Abhängigkeit".
-
-[Anna]
-Notiert.
-
-[Clara]
-Und "keine personenbezogenen Daten in technischen Logs".
-
-[Farid]
-Und "Secrets Management".
-
-[Eva]
-Und "keine Sonderrabatte ohne Freigabe".
-
-[David]
-Und "Supportprozess unklar".
-
-[Anna]
-Wir haben jetzt sehr viele offene Punkte.
-
-[Ben]
-Ja. Für einen Agenten oder ein System, das daraus Requirements ableitet, ist das ein echter Stresstest.
-
-[Clara]
-Wichtig ist, dass Widersprüche nicht einfach glattgebügelt werden. Sie müssen sichtbar bleiben.
-
-[Eva]
-Und Risiken dürfen nicht nur technisch sein, sondern auch finanziell und organisatorisch.
-
-[Farid]
-Architektur darf keine konkreten Cloud-Produkte erfinden, solange wir keinen Provider gewählt haben.
-
-[Anna]
-Genau. Keine Azure-Entscheidung nur weil jemand Azure AD gesagt hat.
-
-[Ben]
-Und OAuth ist bevorzugt, aber noch nicht final.
-
-[Clara]
-DSGVO ist nicht optional.
-
-[David]
-Support ist nicht gelöst.
-
-[Eva]
-Freigaben sind nicht gelöst.
-
-[Anna]
-Dann ist das finale Ziel für jetzt: ein MVP-Schnitt, der klar sagt, was drin ist, was bewusst ausgeschlossen ist und welche Risiken dadurch entstehen.
-
-[Ben]
-Und keine falsche Sicherheit erzeugen.
-
-[Clara]
-Genau.
diff --git a/run-config.json b/run-config.json
index 747ad74..c6d8b72 100644
--- a/run-config.json
+++ b/run-config.json
@@ -5,7 +5,8 @@
  // "phase2ContextStrategy": "message_passing",
   "phase2ContextStrategy": "artifact_state",   // Phase 2.1B: MAF Shared State. Umschalten reicht..Prompts werden automatisch gewählt.
   "llmProvider": "openrouter",
-  "agentModel": "openai/gpt-4.1-mini",
+  "agentModel": "openai/gpt-5.4",   // A'-Lauf: Modell auf gpt-5.4 (einzige Aenderung ggu. 28a52b) -> Modell-Confound isolieren. Danach zurueck auf gpt-4.1-mini.
+ // "agentModel": "openai/gpt-4.1-mini",
  // "agentModel": "openai/gpt-oss-120b:free",
  //  "agentModel": "openai/gpt-oss-120b",
  //langsam: "agentModel": "nvidia/nemotron-3-super-120b-a12b:free",