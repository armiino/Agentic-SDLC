# Architektur

- Das System benötigt eine API-Schicht für die Integration mit verschiedenen Diensten und Tools. OAuth wird als Standardsicherheit verwendet.
- Eine Datenbank-Lösung, basierend auf Managed Services, ohne eigenen DB Server.
- Ein Rollenmodell wird implementiert, um Zugriffskontrolle sicherzustellen.